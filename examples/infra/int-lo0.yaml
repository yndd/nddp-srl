apiVersion: srl.nddp.yndd.io/v1alpha1
kind: SrlInterface
metadata:
  name: int-lo0
  namespace: default
spec:
  active: true
  networkNodeRef:
    name: leaf1
  interface:
    name: lo0
    admin-state: enable
    description: ndd-lo0